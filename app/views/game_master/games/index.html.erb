<div class="container">
  <div class="row">
    <div class="col-xs-4 col-xs-offset-4">
      <div class="form-centered">
        <h1>Games</h1>
        <ul>
          <% @games.each do |game| %>
          <li><%= link_to game.title, game_master_game_rounds_path(game) %></li>
          <% end %>
        </ul>
      </div>
    </div>
  </div>
</div>
